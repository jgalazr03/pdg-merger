/**
 * Tokens de movimiento del motivo "El Orden Instantáneo" (el resolve).
 * Una sola fuente para que el hero de documentos, el de medios y el micro-spinner
 * compartan exactamente la misma curva y cadencia: eso es lo que hace que se
 * sienta "hecho por un solo equipo".
 */

// Asentamiento confiado (ease-out-expo). Sin bounce, sin elastic.
export const RESOLVE_EASE = 'cubic-bezier(0.16, 1, 0.3, 1)';

// Cascada entre trazos al alinearse (ms).
export const RESOLVE_STAGGER = 42;
